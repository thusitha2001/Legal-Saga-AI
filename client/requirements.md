## Packages
react-markdown | Rendering formatted text output from the LLM
remark-gfm | Supporting tables and strikethrough in markdown
framer-motion | Page transitions and interactive animations
date-fns | Formatting dates gracefully

## Notes
- API calls to POST /api/documents take time (LLM processing). UI implements a prominent loading state.
- Risks are categorized by severity (high, medium, low) and mapped to specific visual badges.
- Using Google Fonts: Libre Baskerville (Display) and DM Sans (Body) for a trustworthy, premium legal aesthetic.
